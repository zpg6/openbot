# ADR 0009: Run D1 concurrency probes on disposable deployed resources

- Status: accepted contract, probe not run
- Decision owner: database owner
- Recorded: 2026-08-23

## Decision

OpenBot will test its guarded D1 operations against one newly created disposable Cloudflare D1 database before the D1 schema freeze. Local D1 and Miniflare runs can catch SQL mistakes, but they cannot satisfy this gate.

The probe deploys two writer Workers with distinct script names and version IDs. Both bind the exact same disposable database ID. A third Worker is the synthetic sink and readback service. The sink has its own script name and version ID. Each writer has one narrow service binding to the exact sink script. The report commits the canonical binding configurations. The sink deployment sends 100 percent of traffic to one version, and the awaited RPC response reports that runtime version for comparison with the deployment record. The sink records every request under a new random receipt and never deduplicates requests. A fourth, operator-side driver starts each competing request in a separate process. The driver sends those requests across the network. A `Promise.all` inside one Worker invocation is not evidence of two writers.

All generated names use the recorded safe prefix and a random lowercase suffix. The Cloudflare account and zone IDs are real operational identifiers. The report stores only their domain-separated HMAC commitments. Probe installation IDs, run IDs, calls, receipts, and payloads are synthetic. The probe must reject a production database ID, a reused database, a name outside the prefix, or a resource whose returned ID does not match the create response captured for this probe run.

The writer trigger routes are temporary HTTPS routes protected by Cloudflare Access. They accept one fixed method and path, an Access-authenticated operator, a run-bound one-use request, and no SQL or arbitrary table name. Cloudflare routes do not enforce methods, so each Worker rejects every other method, path, query string, media type, body size, or body shape. `workers.dev` and preview URLs stay disabled. Writers reach the sink through awaited service-binding RPC methods. Access context does not cross that binding, so only the public fetch handlers require Access identity.

The sink/readback Worker also has one temporary Access-protected readback route. It accepts only an exact `GET` path for the generated probe run and returns one fixed response schema. It accepts no SQL, table name, filter, or caller-selected ID. Cleanup first closes a stored run fence so both writers reject new work. Access and all three exact routes remain active until the final first-primary read completes. Cleanup then revokes the service token and removes every route before deleting any Worker.

## Operator preflight

`pnpm d1-probe:plan` is the implemented preflight boundary. It is private Node-only code with no package export. It accepts no arguments, reads one bounded canonical JSON request from standard input, and reads the unpadded HMAC key only from file descriptor 3. It performs no network request, Cloudflare mutation, report assessment, or gate promotion. The emitted plan states that it is non-authoritative and that no deployment occurred.

The operator creates every 16-character lowercase suffix with a cryptographically secure random source before invoking the command. Preflight validates syntax and pairwise uniqueness; it does not treat that validation as evidence of randomness. The request also names one lowercase canonical HTTPS DNS origin with no path, port, credentials, query, or fragment. The plan derives three exact HTTPS path routes under `/_openbot-d1-probe/`: Writer A and Writer B accept `POST`, while readback accepts `GET`. Each route is bound to its generated target script. One Access application path covers only `/_openbot-d1-probe/*`. Worker routes do not end in a wildcard, so query strings do not match them. The deployment driver must verify through Cloudflare that the origin belongs to the supplied zone and has the proxied DNS required by a Worker route before creating those routes.

The plan stores domain-separated HMAC commitments for the origin, Access application path, route patterns, real Cloudflare account and zone, every operator-listed production database ID, and each generated resource name. The lifecycle journal copies the resource commitments from the plan. Every resource event must match the planned name and exact returned resource ID. Database creation stops if the returned database ID commitment matches the operator deny-list.

The later deployment boundary cannot trust a parsed plan by shape. It must recompile the complete plan from the parsed request and HMAC key and compare the canonical bytes. Successful comparison creates an opaque in-memory verified preflight context. Its resolver accepts only the exact object minted by that verifier, returns deeply frozen copies of the request and recompiled plan, and retains no HMAC key. A copied or type-cast object does not resolve. This boundary still performs no deployment and grants no gate authority.

The route precheck accepts only that opaque context and a strict projection of Cloudflare readback. It binds the returned zone ID and owning account to the preflight request. The zone must be active, full, and unpaused. The probe hostname must equal the zone name or be its subdomain. DNS lookup uses the exact-name field and `proxied=true`, captures every page with consistent pagination metadata, and accepts only unique proxied A, AAAA, or CNAME records for that hostname. An empty, partial, mismatched, unsupported, or unproxied result denies.

The credentialed reader calls only Cloudflare's fixed zone-details and DNS-list endpoints with `GET`. It uses a Bearer API token that must have Zone Read and DNS Read for the exact zone. It requests identity encoding, follows no redirect, retries nothing, caps each response at 262,144 bytes, caps all responses at 1,048,576 bytes, and gives the whole read 20 seconds. It discards error bodies and returns no token or raw Cloudflare identifier. `pnpm d1-probe:check-route` accepts no arguments, reads one bounded canonical command from standard input, reads the HMAC key only from dedicated file descriptor 3, and reads the API token only from dedicated file descriptor 4. Both secret descriptors must be pipes or socket descriptors; ordinary files, absent descriptors, environment fallbacks, extra newlines, and oversized values deny. The command first recompiles and verifies the complete preflight plan, then performs the two fixed reads. Standard output contains only the canonical non-authoritative inspection, while standard error contains only fixed denial codes. The command performs no mutation or deployment and cannot promote a gate.

The unwired D1 creation adapter accepts only an opaque route observation minted by that credentialed reader within the previous five minutes. It rechecks the complete plan against the HMAC key and requires the exact planned lifecycle journal before sending one request. The request uses `POST /accounts/{account_id}/d1/database`, the planned disposable name, `read_replication.mode` equal to `auto`, and the jurisdiction bound by preflight unless the operator selected `automatic`. It requires a separate D1 Write token, follows no redirect, retries nothing, caps the response at 262,144 bytes, and gives the call 20 seconds. A successful response must return the exact name, jurisdiction, replication mode, and a valid database ID. The adapter HMAC-commits the ID and advances only `database_created`. It exposes the raw ID solely through an opaque in-memory cleanup target. A deny-list collision, name mismatch, or configuration mismatch becomes `manual_required` with that cleanup target. A lost, malformed, oversized, or otherwise unprovable response becomes `manual_required` without a retry. No root command calls this adapter yet, and its observation cannot attest or promote a gate.

The unwired database bootstrap adapter accepts only that opaque database target and the exact journal prefix containing `database_created`. The disposable SQL lives only at `@openbot/d1-probe-rpc/schema`; both local workerd probes and the operator adapter use its 30 fixed statements and pinned SHA-256 digest. One D1 query batch first requires the probe namespace to be empty, then installs the 28 tables and two triggers and reads back that namespace across tables, triggers, indexes, and views. The query excludes Cloudflare's unrelated reserved storage tables while detecting an unprefixed object attached to a probe table. Three fixed follow-up requests verify `PRAGMA foreign_keys = 1`, force an intentional foreign-key rollback, and prove the canary's first write did not remain. One 20-second deadline covers all four requests. The adapter follows no redirect, retries no mutation, caps each response at 262,144 bytes, and never accepts caller SQL. Success mints an opaque initialized-database observation without advancing `sink_private_shell_created`. A lost or contradictory result marks the exact `database_created` journal `manual_required` and enables only target-bound emergency database deletion. No root command calls this adapter, and its output cannot attest or promote a gate.

Worker provisioning requires the opaque initialized-database result after `database_created`. It records private shell creation, Version upload, and one-Version 100 percent Deployment for the sink, Writer A, and Writer B. Access then follows application, service token, policy, and routes. Each upload observation binds the Version contract, Version-request, artifact, and binding-configuration digests. Each Deployment must name that same uploaded Version and those same digests, bind its separate no-force Deployment-request digest, and record `force: false`. The generic journal function returns `opaque_evidence_required` at all nine Worker mutation steps, and the plain journal schema rejects every caller-fabricated Worker-mutation prefix. A database-bootstrap failure before a Worker request may use `unexpected_platform_result` at `sink_private_shell_created` only with the distinct `pre_dispatch` phase. Once a shell, Version, or Deployment request is dispatched, only its specific ambiguity reason is accepted. The outcome is unknown, retry is denied, and manual cleanup is required.

The pure Worker provisioning protocol uses reviewed projections from Cloudflare's beta Worker resource API and classic Scripts API. Shell creation fixes the subdomain and previews to false, disables logpush and observability, sets no tail consumer, and requires empty Version and Deployment lists before upload. Version creation fixes `deploy=false`, exact module bytes and bindings, deterministic markers, empty beta URLs, and classic `hasPreview: false`. Deployment fixes one Version at 100 percent and `force=false`, then requires another false/false subdomain readback and matching runtime Version Metadata. Identity commitments use the report's shared role-free HMAC domains and key identifier. Shell creation time must fall inside the bounded dispatch and observation window. The returned record retains planned names, request metadata, timestamps, counts, and safety booleans, but exposes no Cloudflare-assigned ID, API token, HMAC key, or module bytes. It grants no upload, lifecycle, attestation, or gate authority.

The first credentialed Worker API check is a separate isolated canary defined by `docs/fixtures/cloudflare-worker-api-canary.json`. Its checked-in status is `not_run`. It creates one disposable private shell and uploads one fixed inert no-binding `entry.js` module through the beta API with `deploy=false`. It then requires classic Version readback, one classic 100 percent Deployment with `force=false`, beta deletion of the exact immutable Worker ID, an exact Worker-ID miss, and a complete list with neither the returned ID nor generated name. It creates no D1 database, binding, Access resource, or route and does not invoke the Worker. It may change only the `beta_classic_api_canary_passed` conclusion after review; `access_protected_runtime_identity_canary_passed` remains false until the later Access-protected runtime canary. Neither conclusion is lifecycle or gate evidence by itself.

The credentialed runner implementation has no root or package command. Its former executable module returns only `worker_api_canary_disabled` before reading input or credentials. It remains unavailable until durable orchestration, cleanup resumption, response capture, and a secure secret-descriptor launcher exist. The retained library source accepts a canonical persisted plan, an HMAC key, and an account-scoped Workers Scripts Write token. One transport limits forward requests to GET and POST and cleanup requests to GET and DELETE. Both share one redacted transcript and one two-megabyte aggregate response budget. The runner derives the key identifier from the actual HMAC key, checks the five-minute plan window on each request, follows no redirect, requests identity response encoding, bounds each response, and dispatches each mutation at most once. A checkout-local exclusive reservation limits accidental replay on one operator checkout but is not a distributed lock. Each execution adds a fresh random ownership tag. Reconciliation and cleanup require the immutable Worker ID, generated name, persisted ownership marker, execution tag, and private settings, so a concurrent same-plan process cannot adopt another process's Worker. Before deletion, cleanup repeats that exact readback. It never retries a Version, Deployment, or delete mutation. An unacknowledged deletion cannot become an observed candidate even if later reads show absence. The library result contains the planned synthetic name, HMAC identity commitments, request and response digests, and mutation counts. It contains no assigned Cloudflare ID, raw execution tag, token, HMAC key, response body, or module bytes. The result remains untrusted and cannot authorize another mutation.

The non-mutating `pnpm d1-probe:plan-worker-api-canary` command generates that canonical plan from an account request, a fresh clock and random source, and the HMAC key on file descriptor 3. It has no token or network capability. The package also contains unwired prerequisites for a later safe execution path: a strict response projection, a canonical checkout-local operation record, a revision-checked `0600` state store, and an automatic cleanup core. The record persists the fresh execution tag and returned immutable identifiers but no token or HMAC key. The state store durably fences `delete_dispatching` before a DELETE in that checkout. The cleanup core is limited to the separately derived automatic grace, exact ownership, complete consistent list metadata, one acknowledged immutable-ID delete, and exact plus complete-list absence. It cannot perform a create, Version, or Deployment request. `pnpm d1-probe:inspect-worker-api-canary-recovery` performs a strictly read-only inspection of an existing local journal. It receives no credential, does no network I/O, refuses missing or unreconciled state instead of changing the filesystem, and emits only the plan digest, revision, state, retained-identity booleans, fixed recovery status, and false authority flags. It never emits the Worker name, resource IDs, ownership tags, execution tag, state path, or stored record. The credentialed runner does not consume these prerequisites yet. No secure launcher, cleanup CLI, post-grace manual authority, response-preimage capture, or reviewed-observation fixture transition exists. Therefore no live invocation is approved.

This canary is deliberately not the production artifact path. Production identity uses the shared `beta_worker_json_version_v1` contract. It derives one canonical beta JSON Version body and bare role-bound binding-configuration, artifact, and `deploy=false` Version-request digests. The artifact candidate and provisioning protocol consume the same projection, and lifecycle upload and Deployment observations require the same contract and digests. Cross-role vectors cover the sink, Writer A, and Writer B. This local compatibility does not make upload safe. The isolated canary is still `not_run`, first-Version preview safety is unresolved, and no credentialed production adapter or opaque evidence consumer exists.

The protocol does not resolve either live platform dependency. Cloudflare marks the Worker resource API beta. The isolated canary covers only beta-to-classic control-plane interoperability and cleanup. The later production-bound canary must separately prove that Access-protected execution returns matching runtime Version Metadata after the exact artifact and digest chain is resolved. No credentialed production shell, upload, Deployment, or cleanup adapter exists. No opaque evidence consumer or gate authority exists.

The matching unwired deletion adapter accepts only that opaque created-database target. Normal cleanup requires the exact journal prefix ending immediately before `database_deleted`; emergency cleanup requires the exact target-bound database-create or database-bootstrap failure journal. It rechecks the plan, HMAC key, database ID commitment, and database name commitment before one `DELETE /accounts/{account_id}/d1/database/{database_id}` request with a D1 Write token. It follows no redirect, retries nothing, caps the response at 262,144 bytes, and gives the call 20 seconds. The target becomes one-use when dispatch begins, including when the response is lost. A successful API wrapper records only `sdk_acknowledged` and advances `database_deleted` during normal cleanup. It explicitly records `absence_verified: false`; later control-plane readback must establish absence before cleanup can be confirmed. An ambiguous response becomes `manual_required` without a retry. Emergency acknowledgement does not clear the pre-existing manual-review state. No root command calls this adapter, and neither its request nor its acknowledgement can attest or promote a gate.

The unwired absence adapter uses Cloudflare's documented D1 list endpoint with the exact generated name as the search input. It does not infer deletion from an undocumented `404` error body. The adapter requires the opaque created target, a recorded delete dispatch outcome, the matching lifecycle journal, the HMAC key, and a D1 Read token. It then reads consecutive pages of 100 results, with at most four pages, 262,144 bytes per page, and one 20-second deadline. Every page must have consistent pagination metadata and unique database IDs. Finding either the exact returned ID or the exact generated name returns `database_still_present`. A complete bounded search without either value records `control_plane_absence_observed` and mints an opaque in-memory observation. Its private context binds the exact created-target object, plan digest, journal digest, ID and name commitments, delete outcome, and observation digest. A forged object or serialized copy does not resolve. The matcher also rejects another target or changed journal. This is a Cloudflare control-plane observation, not independent proof. It does not advance `all_resource_absence_confirmed`, clear a manual-review state, attest a gate, or finish cleanup. The generic lifecycle function rejects that final step with `evidence_required`, and the untrusted journal schema rejects a caller-built terminal journal. A later aggregate cleanup operation must consume the exact opaque observation together with equivalent observations for every other resource. That consumer and its one-use rule do not exist yet. A failed read may be invoked again because it has no side effect; the adapter performs no automatic retry.

The journal accepts only the fixed create and cleanup prefix below. Skipped, repeated, renamed, or substituted resources deny. An ambiguous create, delete, in-flight state, or absence observation moves the journal to terminal `manual_required`; the operator cannot continue the same plan blindly. This preflight and journal make a later deployment driver safer, but they do not implement that driver or close a D1 gate.

## Private receipt RPC

`packages/d1-probe-rpc` defines the versioned writer-to-sink request. Its digest binds the probe run, writer role, source request, and one typed payload. Version 1 supports a private-RPC setup probe, a gateway dispatch receipt, and an exact Sandbox destroy observation. It accepts no SQL, table name, URL, or arbitrary command. Writer A and Writer B use separate Worker entrypoints and reject a request for the other role before calling the sink.

The sink inserts every valid request into `_openbot_probe_external_sink_receipt` under a fresh random receipt ID. The table has no uniqueness constraint on the call, attempt, reservation, request digest, or payload. Repeating an identical valid request therefore creates another receipt. A malformed request denies before D1. A missing or malformed result after the D1 call starts returns `outcome_unknown`. A lost private-RPC response also returns `outcome_unknown`; the writer does not call the sink again.

The local-only writer entrypoints also implement the gateway-reservation operation over disposable `_openbot_probe_external_gateway_*` tables. The request digest binds the probe run and scenario, writer role, exact-or-substituted variant, call kind, logical call, attempt, sequence, reservation, dispatch digest, and one named fault point. A three-statement `first-primary` batch conditionally inserts the reservation, inserts its foreign-key guard, and decrements the one-call budget. The writer validates every `RETURNING` row and D1 metadata field and requires a non-null session bookmark before dispatch. Only the committed writer calls the sink, exactly once, after the batch. A recognized losing constraint is classified only after one fresh first-primary aggregate readback proves the stored reservation and guard. An identical envelope becomes `same_digest_replay`; a second writer envelope for the same exact dispatch becomes `duplicate_dispatch_denied`; a substituted dispatch digest or binding becomes `changed_digest_denied`. Unknown D1 state, missing bookmarks, malformed results, or failed reconciliation never become a denial or retry.

The two fixed fault points preserve uncertainty. `reserve_then_crash` returns after the commit and before the sink call. `dispatch_response_lost` awaits one sink call and then discards its response. A thrown, missing, or malformed sink response becomes `outcome_unknown`, and the writer never calls the sink again. A typed pre-effect sink rejection or unavailability records `reserved_without_dispatch`. These local entrypoints exercise the intended deployed operation and dry-run bindings, but they expose no trigger route, create no deployed report, and cannot promote a gate.

The checked-in Wrangler files are local-only build inputs. They disable `workers.dev`, preview URLs, logs, and routes. Both writers bind one local D1 database and the exact `D1ProbeSinkService` entrypoint. Writer A and Writer B now use separate role-only source modules. Their default fetch handlers implement the exact Access-aware trigger contract, while the sink implements the exact Access-aware fixed readback. Local configuration injects only synthetic URLs and service-client identities at build time. `pnpm d1-probe:build-local` performs dry-run bundles only and has no deploy mode. The later deployment driver must generate run-bound values and create the exact routes, Access resources, collector, and cleanup. No checked-in route exposes these handlers, and they cannot close a gate.

The private operator package can compile an untrusted Worker artifact candidate for the sink, Writer A, and Writer B. The shared beta Version contract hashes the exact module bytes and canonical JSON request body, including the `database_id` D1 field, initialized run database, generated sink service name, compatibility date `2026-08-22`, no compatibility flags, annotations, and exact D1, service, and version-metadata bindings. A role-and-caller-bound binding-configuration digest distinguishes Writer A from Writer B even when their module bytes match. The artifact digest binds the complete beta body, and the Version-request digest also binds the fixed method, path template, and `deploy=false` query. The candidate additionally binds untrusted caller claims for the build recipe, package digests, Node 22.19.0, pnpm 11.22.0, Wrangler 4.125.0, disabled observability, role-specific source entrypoints, role-only exports, Access fetch behavior, and a runtime-version response contract that has not run on Cloudflare. These fields are not proved remote state.

This beta direct-API contract is not a byte mirror of Wrangler 4.125.0 output. Later use of Wrangler output needs a reviewed normalization step. The local dry-run bundles still emit source maps and use synthetic build-time HTTP configuration, and no runtime-version observation has run on Cloudflare. They therefore remain ineligible for upload. The role-only exports and Access-aware fetch handlers are implemented locally. Unit tests simulate successful verified Access contexts. Workerd tests prove distinct role-only RPC modules and unauthenticated fetch denial, but do not simulate Access authentication or a deployed route. A successful candidate remains `eligible_for_upload: false` and `deployment_ready: false`. It exposes no module bytes or raw database ID and cannot upload, deploy, advance the lifecycle, attest, or promote a gate. No upload adapter exists. Version creation and Deployment need separate lifecycle steps, ambiguous-outcome reconciliation, deployment and runtime readback, and cleanup binding before any artifact can become deployment-ready.

The Writer HTTP adapter is wired into each local role-only entrypoint without creating a route. Bootstrap configuration pins one canonical HTTPS URL, one Access audience, one Access service-client identity, and one writer role. The adapter accepts only an exact `POST` with unencoded `application/json`, an exact content length, and at most 16,384 canonical UTF-8 bytes. It rejects query strings, alternate paths, duplicate JSON keys, noncanonical JSON, role substitution, and malformed execution results. It reads only Cloudflare's verified `ctx.access` audience and service-token identity. It does not parse an unverified Access header. The deployed entrypoint must receive the context from the directly authenticated request because Access context does not cross the private sink binding.

The private Node-only driver transport sends the matching request. It uses Cloudflare's standard `CF-Access-Client-Id` and `CF-Access-Client-Secret` headers, sends one canonical body, follows no redirects, and makes no automatic retry. Its timeout remains active while it reads the response body. The response cap is 65,536 bytes. Every response produced after Access authentication must carry one canonical base64url runtime-version header containing the exact version ID, tag, and timestamp. The driver strictly decodes and returns that observation for the collector; later deployment evidence must bind it to the expected version. Pre-Access `not_found` and `access_required` responses must omit the header. A response is usable only when its status, request digest, writer role, canonical JSON bytes, runtime-version rule, and no-store headers match the Writer contract. A network error, timeout, redirect, oversized body, invalid UTF-8, or substituted response becomes `outcome_unknown`. The transport returns no credential field or exception text.

The package also contains a parent-only child command; it is not a root operator command. The child requires Node IPC and reads one canonical assignment of at most 32,768 bytes from standard input. It installs its message listener, sends one `READY` message, and waits at most 10 seconds for one `GO` message that binds its child-process ID, writer role, request digest, and GO-receipt digest. It reads the Access Client Secret from file descriptor 4 only after that exact `GO`, accepts at most 512 secret bytes plus one optional trailing newline, and closes the descriptor before it reports. It then invokes the one-request transport once and emits one canonical result. Invalid assignment, missing IPC, substituted `GO`, unavailable credentials, or malformed result fails locally without a network request or automatic retry. No root command, public route, deployment, stored credential, or gate-promotion path exists in this slice.

The gateway parent coordinator validates one fixed Writer A and Writer B pair before it starts either trial. Both assignments must bind the same probe run, trial, call kind, logical call, attempt, sequence, reservation, Access service-token identity, Writer origin, and request timeout. Child IDs, trial requests and digests, GO receipts, gateway request IDs, and Writer routes must differ. The Node adapter starts two operating-system children with dedicated IPC and credential pipes. The coordinator waits at most 5 seconds for both exact `READY` messages before it attempts either `GO`. It then waits at most 20 seconds for each bounded canonical child result. A partial spawn, failed READY, failed GO send, timeout, early exit, stderr byte, excess output, or substituted result terminates the children and returns a non-authoritative `inconclusive` result. Termination gets 1 second before the adapter uses `SIGKILL`. A `completed` result means only that both child protocols completed; it does not mean the D1 history passed.

The private package parent command reads one canonical assignment of at most 65,536 bytes from standard input. It accepts no arguments. It reads the Access Client Secret from file descriptor 3, accepts at most 512 secret bytes plus one optional trailing newline, and closes that descriptor before it starts a child. It accepts no credential argument or environment variable. The child processes inherit no environment from the parent. `SIGINT` and `SIGTERM` abort the coordinator, terminate every spawned child, and produce an inconclusive result instead of exiting around cleanup. The command emits one canonical result. `completed` exits 0, `inconclusive` exits 2, and invalid local input exits 1 with a fixed code on standard error. This package command is not registered at the repository root and performs no deployment.

The local Writer RPC also implements the gateway trial's one-use admission and readiness barrier. The outer request digest binds the child-process ID, writer role, GO receipt, exact two-contender count, fixed 2,000-millisecond scheduled-wait budget, fixed 25-millisecond poll interval, and the already verified gateway request. The operator must seed one exact assignment per child before invocation. One D1 batch inserts readiness through that assignment and an open trial row, then inserts a foreign-key guard. Readback requires one matching guard for every readiness row. A replay, changed request, missing assignment, or closed trial denies only after fresh primary readback. Unknown D1 state stays unknown.

After readiness commits, the writer opens a new `first-primary` session for each poll. The aggregate read must show one open trial, exactly two assignments, two readiness guards, and a sorted unique ready-child set. The writer enters the gateway reservation only after that set contains both children. Eighty unsuccessful polls exhaust the 2,000-millisecond scheduled-wait budget, return `barrier_timeout`, and make no sink call. D1 query latency is additional; the deployed parent must enforce the hard request deadline. Poll failure or a failed local wait returns `outcome_unknown`. This RPC has no public trigger route and does not prove that two separate operating-system processes or network requests participated. The later driver and deployed report must still prove those facts.

Miniflare D1 does not return the deployed primary, region, timing, or attempt metadata required by the probe contract. The Workerd integration test therefore expects both readiness inserts to commit and both writer responses to stop at `readiness_d1_unknown`. Its first-primary readback verifies two distinct child, writer-role, GO-receipt, and operation-request identities, with zero gateway reservations and zero sink receipts. Unit tests use explicit metadata fixtures to exercise barrier release, timeout, replay, substitution, and read failure. Neither test can stand in for the deployed D1 run.

## D1 execution rules

Every writer creates a D1 session with `withSession("first-primary")`. It issues each guarded operation once. OpenBot adds no application retry. D1 may retry read-only queries, so the collector records `total_attempts` and does not require it to equal one. For a committed batch, the writer returns the session bookmark, each `RETURNING` row, and the `D1Result` metadata for every statement. A recognized rejected batch need not produce a bookmark; its fresh first-primary no-partial-write readback is decisive. The collector shape-checks and records result count, success flags, changes, rows read, rows written, database-change flag, `served_by`, primary-serving flag, region, duration, SQL duration, total attempts, last row ID, and database size. Metadata counters are diagnostic. They do not prove which row changed. Exact `RETURNING` identity and cardinality, followed by fresh first-primary readback, decide the history.

An intentional tripwire rollback returns a recognized D1 constraint failure instead of successful statement metadata. The collector must distinguish that expected rejection from an unknown failure and prove through first-primary readback that the batch left no guarded rows. A missing, malformed, contradictory, or lost Worker, sink, D1 result, or D1 error response makes the trial inconclusive.

Setup enables D1 read replication and reads that setting back from the deployed database. The required `served_by_primary` metadata field must equal `true`. An absent or false value makes the trial inconclusive. Setup also reads back foreign-key enforcement and runs an isolated failing tripwire canary. First-primary readback must show that the canary batch left no rows. A probe cannot rely on guarded batches until both setup checks pass.

Only the first query in a `first-primary` session is guaranteed to use the primary. Each decisive snapshot therefore uses one aggregate SQL statement in a fresh session, or one fresh session for every `SELECT`. The collector also opens a session from each returned writer bookmark and checks that writer's receipt. Bookmark reads are supporting evidence. Only the fresh first-primary query decides the final history. The report records the configured read-replication setting, observed `served_by_primary: true` metadata, compatibility date, committed database ID, script IDs, deployment IDs, runtime-observed version IDs, bookmarks, and result metadata.

The parent driver forks one child per contender. Guarded-create, gateway, and audit trials use two children. Capacity uses five. Each child signals local readiness over IPC and waits. The parent sends one `GO` message to each child and records one receipt per child. Each child sends exactly one network request to its assigned Worker. Each Worker records a readiness row tied to that child. The report binds the child, readiness-row, `GO` receipt, and network-request identity sets. Each Worker polls with fresh first-primary sessions until every contender is ready before running its guarded batch once. The audit trial also records the same expected sequence and head hash. A timeout, worker restart, lost writer or sink response, unknown platform result, malformed response, or missing ready row is inconclusive. The driver does not rerun the same request ID to turn an ambiguous result into a pass.

## Guarded create and revoke

Probe-only tables model an active authority row, a live-confirmation slot, a confirmation, a run, a cancellation outbox row, and an assertion row whose foreign key points at the run. They are not product migrations.

The create writer executes one D1 batch. It conditionally consumes the exact confirmation and clears its live-confirmation slot while authority is active, inserts the run, and inserts the assertion. The assertion is an intentional foreign-key tripwire. If the guarded statements did not create the exact run, the tripwire fails and D1 rolls back the batch.

Revoke invalidates the dependent confirmation and clears its live-confirmation slot. If the run already exists, revoke changes it to `cancellation_requested` and inserts one cancellation outbox row in the same guarded batch. The probe forces and then verifies these histories:

1. Create commits first. It consumes the confirmation and clears the live-confirmation slot. Revoke commits second. The first-primary read shows the consumed confirmation, a clear slot, one `cancellation_requested` run, one assertion, one cancellation outbox row, and revoked authority.
2. Revoke commits first. It discards the dependent confirmation and clears the live-confirmation slot. Create reaches the tripwire and the batch rolls back. The first-primary read shows the discarded confirmation, a clear slot, no run, no assertion, no cancellation outbox row, and revoked authority.
3. An equal-release race produces exactly one of those two histories. Any third state fails.

The race repeats with writer roles swapped. The report records both deployment identities for every trial.

## Gateway reservation

The probe runs the same contention case for `model`, `provider_tool`, and `code`. Two writers receive the same synthetic call kind, logical call ID, attempt ID, sequence, request digest, and reservation key. A unique reservation plus a foreign-key tripwire lets exactly one writer commit the spent reservation. Only that writer may call the sink.

The sink records every received request before replying. It creates a new random receipt for each request and does not deduplicate on call kind, call ID, attempt ID, reservation ID, or request digest. The first-primary read must show one spent reservation and one sink receipt. The losing writer must show a guarded denial and no outbound call.

Each call kind also runs a changed-digest contention case. One exact request may commit. The request with the substituted digest must receive a guarded denial and must not create another sink receipt.

Two fault trials are mandatory. In the reserve-crash trial, the winning writer stops after the reservation commit and before the sink call. Readback must show one spent reservation and zero sink receipts. The outcome is `outcome_unknown`; no retry occurs. In the response-lost trial, the sink records one receipt but its response never reaches the writer. Readback must show one spent reservation and one receipt. The outcome remains `outcome_unknown`; no retry occurs. These trials prove that the reservation prevents duplicate dispatch. They do not claim delivery certainty.

## Sandbox capacity

The disposable database starts with a capacity limit of four and zero claims. Five independent requests, split across both writer deployments, contend for distinct synthetic claim IDs. A conditional claim plus a foreign-key tripwire must commit four claims and deny the fifth before any synthetic platform call.

Capacity release requires a sink-recorded destroy observation bound to the exact installation ID, run ID, run-attempt fence, claim ID, sandbox ID, and random destroy receipt. Separate typed operations substitute each value in turn. Release before observation, every mismatched target, and replayed release must not change capacity. One matching release decrements the reserved count once, after which the fifth claim commits. An unknown or lost destroy response does not count as an observation and leaves the claim reserved for manual review.

## Audit head

Both writers read the same first-primary audit head, record the same expected sequence and hash, and stop at the barrier. They propose different synthetic event IDs and hashes. After release, one guarded batch appends the next event and advances the head. The other conflicts and rolls back. First-primary readback must show one next-sequence event and its matching head.

The loser then performs a separate follow-up command. It reads the new head, uses a new attempt ID, appends at the next sequence, and commits. This is an explicit second phase, not an automatic retry of an ambiguous request. Separate operations prove that a stale sequence, a gap, and the wrong previous hash are denied without splitting the head from the event chain. Final readback must show an unbroken two-entry chain.

## Evidence and authority

The checked-in fixture at `docs/fixtures/d1-concurrency-probe.json` is a deployment contract. Its status is `not_run`. It contains no observations and cannot promote `d1_guarded_create` or `gateway_reservation`. The preflight plan, lifecycle journal, local bundle, and private RPC response also carry no such authority.

A deployed collector emits either a strict candidate-pass report or a typed `inconclusive` or `manual_required` failure envelope. The candidate report uses `platform` equal to `cloudflare_d1_deployed` and required check-set version 1. Its pre-run deployment projection commits the Cloudflare account and zone, exact database, Worker scripts, deployments, routes, Access resources, binding configurations, generated-name guards, and operator database deny-list. Setup and runtime observations remain outside that deployment digest. The report also binds the probe configuration, collector build, installation, environment, probe run, final current-state digest, cleanup outcome, and redacted transcripts. Each identity type uses one shared domain-separated HMAC preimage across all roles, so reusing one script cannot produce distinct commitments. The assessment recomputes the deployment and final-state digests and compares every context digest with operator-supplied expected values. Every required trial must finish without an inconclusive result. Failure envelopes are valid records but never eligible for review. The pure gate-evidence assessment must return `eligible_for_operator_review` before the offline operator command may sign a candidate report. That result is not authority. A report, fixture, local run, assessment result, or `passed` field has no authority by itself.

The typed gate-evidence contract requires Sandbox-capacity and audit-head checks under the D1 gate, plus separate `model`, `provider_tool`, and `code` coverage under the gateway gate. Schema freeze remains blocked until a deployed report passes those checks.

## Cleanup

Cleanup uses only the exact resource IDs returned during this probe and checks every generated name against the recorded prefix.

1. Close the stored probe-run fence. Reject new trial starts while retaining Access on every active route.
2. Wait for the recorded in-flight request IDs to finish or mark cleanup `manual_required` if their state is unknown.
3. Call the retained exact readback route. It runs the final first-primary query for the current state and returns only the fixed synthetic response. Recompute the canonical final-state digest. Store a redacted HMAC transcript commitment plus a typed response projection that names that digest; never equate the HMAC with the raw digest.
4. Revoke the Access service token.
5. Delete the three exact route IDs and confirm their absence.
6. Delete the exact Access application and reusable policy.
7. Delete the two writer scripts without `force`.
8. Delete the sink script after its service-binding callers are gone. Service bindings disappear with their caller configurations, so cleanup does not treat them as separate resources.
9. Delete the exact disposable D1 database last.
10. Read the Cloudflare control plane and confirm that every recorded resource ID is absent.

A lost delete response, ID mismatch, unexpected resource, failed absence check, or unknown in-flight request sets cleanup to `manual_required`. The collector must not claim cleanup success from a request alone.

## Sources

- [D1 database API](https://developers.cloudflare.com/d1/worker-api/d1-database/)
- [D1 sessions API](https://developers.cloudflare.com/d1/worker-api/d1-database-session/)
- [D1 return objects](https://developers.cloudflare.com/d1/worker-api/return-object/)
- [D1 retry behavior](https://developers.cloudflare.com/d1/best-practices/retry-queries/)
- [Workers service bindings](https://developers.cloudflare.com/workers/runtime-apis/bindings/service-bindings/)
- [Workers version metadata](https://developers.cloudflare.com/workers/runtime-apis/bindings/version-metadata/)
- [Workers routes](https://developers.cloudflare.com/workers/configuration/routing/routes/)
- [Cloudflare Access for Workers](https://developers.cloudflare.com/workers/configuration/cloudflare-access/)
- [Cloudflare Access application paths](https://developers.cloudflare.com/cloudflare-one/access-controls/policies/app-paths/)
- [Cloudflare Access application tokens](https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/application-token/)
- [Cloudflare Access service tokens](https://developers.cloudflare.com/cloudflare-one/access-controls/service-credentials/service-tokens/)
- [Workers script deletion](https://developers.cloudflare.com/api/resources/workers/subresources/scripts/methods/delete/)
- [Cloudflare Access service policies](https://developers.cloudflare.com/cloudflare-one/access-controls/policies/common-policies/)
- [Cloudflare zone details](https://developers.cloudflare.com/api/resources/zones/methods/get/)
- [Cloudflare DNS record list](https://developers.cloudflare.com/api/resources/dns/subresources/records/methods/list/)
- [Cloudflare API token creation and scoping](https://developers.cloudflare.com/fundamentals/api/get-started/create-token/)
- [Create D1 database](https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/create/)
- [Delete D1 database](https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/delete/)
- [List D1 databases](https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/list/)
- [D1 read replication](https://developers.cloudflare.com/d1/best-practices/read-replication/)
