# Workspace handoff

After a major visual update, give the user a clickable link that opens the Finder folder containing the generated screenshots and videos. Run the visual test first so the linked folder contains the current output.
