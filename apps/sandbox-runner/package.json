{
    "name": "@openbot/sandbox-runner",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "wrangler deploy --dry-run --outdir ../../.build/workers/sandbox-runner --config wrangler.jsonc --env preview"
    },
    "dependencies": {
        "@openbot/sandbox-protocol": "workspace:*"
    }
}
